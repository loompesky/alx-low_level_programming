#ifndef MATH_H
#define MAIN_H

#include <stdio.h>

/*The function test if an integer is positive or negative*/
void positive_or_negative(int i);

/*The function that returns the largest of 3 numbers*/
int largest_number(int a, int b, int c);

int convert_day(int month, int day);

void print_remaining_days(int month, int day, int year);

#endif /* MAIN_H */

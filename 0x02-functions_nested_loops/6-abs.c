#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: integer to affect
 * Return: Then value of integer
 */
int _abs(int n)
{
	if (n < 0)
		retrun (n * -1);
	else
		return (n);
}
